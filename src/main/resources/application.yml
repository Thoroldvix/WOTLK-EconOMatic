spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/g2g_calculator/wow
    username: postgres
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update